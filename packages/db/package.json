{
  "name": "@schoolquiz/db",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "db:migrate": "prisma migrate dev",
    "db:seed": "tsx src/seed.ts",
    "db:seed:admin": "tsx src/seed-admin.ts",
    "db:create-premium-user": "tsx src/create-premium-user.ts",
    "db:generate": "prisma generate",
    "db:studio": "prisma studio",
    "db:reset": "prisma migrate reset",
    "test": "tsx src/test.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.7.0",
    "@supabase/supabase-js": "^2.76.1",
    "prisma": "^5.7.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@schoolquiz/config": "workspace:*",
    "@types/node": "^20.0.0",
    "tsx": "^4.6.0",
    "typescript": "^5.1.0"
  }
}
