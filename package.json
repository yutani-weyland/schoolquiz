{
  "name": "schoolquiz",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "clean": "turbo run clean",
    "postinstall": "node scripts/patch-radix-slot.js",
    "sync-env": "node scripts/sync-env.js",
    "setup": "node scripts/sync-env.js && echo '✅ Environment files synced'"
  },
  "devDependencies": {
    "@turbo/gen": "^1.10.12",
    "turbo": "^1.10.12"
  },
  "packageManager": "pnpm@8.6.10",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "lucide-react": "^0.294.0"
  },
  "pnpm": {
    "patchedDependencies": {
      "@radix-ui/react-slot@1.2.4": "patches/@radix-ui__react-slot@1.2.4.patch"
    },
    "overrides": {
      "@radix-ui/react-slot": "1.2.4"
    }
  }
}
