/**
 * Admin template
 * This component wraps all admin pages and re-renders on navigation
 */
export default function AdminTemplate({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}

